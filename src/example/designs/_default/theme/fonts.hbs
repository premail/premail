<!--

ABOUT THIS FILE

fonts.hbs is a mix of Handlebars and MJML template languages, and controls
MJML's inclusion of custom fonts. Generally it will be easier to change these
settings directly in your designConfig.yaml file, but the markup is included
here for clarity.

-->

{{#if design.fonts.web}}
<!-- HREF values in the font element are unquoted, and ignored by Prettier,
     in order to allow for the double-quoting that JSON-to-Sass variables
     require. -->
<!-- Google fonts -->
{{#if design.fonts.stack.google.enabled}}
<!-- prettier-ignore -->
<mj-font
            name="{{design.fonts.stack.google.name}}"
            href={{{design.fonts.stack.google.href}}}
          />
{{/if}}
<!-- Custom fonts -->
{{#if design.fonts.stack.custom.enabled}}
<!-- prettier-ignore -->
<mj-font
            name="{{design.fonts.stack.custom.name}}"
            href={{{design.fonts.stack.custom.href}}}
          />
{{/if}}
<!-- End of web fonts -->
{{/if}}
