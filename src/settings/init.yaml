# Settings loaded for `premail init`

# Default directory structure
dirs:
  design:
    name: designs
    default: _default
    theme:
      dir: theme
  email:
    name: emails
  output:
    dir: dist
